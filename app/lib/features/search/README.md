# features/search

Restaurant search & resolution (Session 2). Typed name → Google Places →
canonical `restaurants` row keyed by `place_id`. Places key stays server-side
in the `resolve-restaurant` Edge Function.
