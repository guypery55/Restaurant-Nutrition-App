# features/menu

Menu display + assessment basket (Session 5). Renders the stored menu grouped
by section in Hebrew/RTL; tap to add/remove dishes to a local "assessment"
basket. No estimation happens here — only on the "Check (N)" button press.
